# M0 Exit Gate：可信工程基线

> 执行日期：2026-08-06（Asia/Shanghai）
>
> 本地验收候选：`e7e4fd87a795a2cc08170c67ab796d88121e1707`
>
> 远端验收提交：`06a841d85efad1c2258c8c6213e48e66a16187b1`
>
> 状态：**Passed — Task 10 Done，M1 / Task 11 Ready**

## 1. Gate 结论

Task 1～9 建立的构建、配置、安全示例、保护测试和质量门禁，已在同一个 detached Windows 全新工作树上重新验证。本地构建、启动、Test、Race、Vet、Staticcheck、Actionlint、前端质量、Secret Scan 和工作树洁净性全部通过。

首次在 `c3a9cd9` 上的全新 Windows 检出暴露了 Golden Fixture CRLF/LF 差异，`TestLoadErrorGolden` 真实失败。该根因由 Task 10.R1 在 `e7e4fd8` 修复，随后使用第二个全新工作树从头重跑 Gate，没有把失败归类为环境问题或修改断言造绿。

Task 9、Task 10.R1 和 Gate 报告已推送。GitHub-hosted [`Quality` Workflow #31084263262](https://github.com/aqww5941-wq/ai-gateway/actions/runs/31084263262) 在报告提交 `06a841d85efad1c2258c8c6213e48e66a16187b1` 上完成：

- Go quality：`success`；
- Frontend quality：`success`；
- Secret scan：`success`。

因此 Task 10 完成、M0 通过，M1 / Task 11 正式释放。该放行只代表“可信迁移基线”，不代表生产就绪或厂商能力已验证。

## 2. 验证环境

| 项目 | 值 |
| --- | --- |
| Windows | Windows amd64，PowerShell 7.6.4 |
| Windows Go | go1.26.5 |
| WSL Race Go | go1.26.4 linux/amd64 |
| Node.js / npm | v22.14.0 / 10.9.2 |
| Git EOL | `core.autocrlf=true`；`*.golden` 与 `*.sh` 固定 LF |
| 新检出 | detached worktree @ `e7e4fd8`；初始无 `bin/` 和 `web/node_modules/` |
| 网络边界 | npm/Go Tool 依赖下载；不访问真实 LLM Provider |

## 3. Exit Gate 结果

| Gate | 结果 | 证据 |
| --- | --- | --- |
| 全新检出 | 通过 | detached HEAD 工作树初始干净，无依赖和本地二进制 |
| 跨平台完整构建 | 通过 | `go run ./cmd/build` 执行 `npm ci`、TypeScript/Vite 与 Windows Gateway Build |
| 构建可重复 | 通过 | 连续两次完整构建，18 个嵌入产物 SHA-256 不变 |
| 工作树洁净 | 通过 | 构建、前端质量与启动 Smoke 后 `git status --porcelain` 为空 |
| 默认安全配置启动 | 通过 | SPA 200，`/metrics` 200，无 admin 身份的 `/admin/health` 403，退出后无残留监听 |
| Go Format/Test | 通过 | `go run ./cmd/quality`；`go test -count=1 ./...` |
| Config/Reload 保护 | 通过 | 严格 Loader、Golden、原子 Snapshot、restart-required 与并发 Reload 测试进入全仓 Test/Race |
| Store/Key/Quota 保护 | 通过 | Migration、Key 生命周期、Quota 并发与故障语义测试进入全仓 Test/Race |
| Provider/Server 保护 | 通过 | 离线 Unary/SSE/Auth/Route/Retry/Fallback/Error/Cancel Fixture 进入全仓 Test/Race |
| Race | 通过 | WSL `go test -race -count=1 ./...` |
| Vet/Static Analysis | 通过 | `go vet ./...`、Staticcheck v0.7.0、Actionlint v1.7.12 |
| Frontend | 通过 | ESLint、2 个测试文件/6 个测试、TypeScript、Vite Build |
| Secret Scan | 通过 | Gitleaks v8.27.2，无泄漏；默认示例无真实调用权限 |
| GitHub-hosted CI | 通过 | [`Quality` #31084263262](https://github.com/aqww5941-wq/ai-gateway/actions/runs/31084263262)，Head `06a841d85efad1c2258c8c6213e48e66a16187b1`；三个 Job 均为 `success` |

## 4. 启动与 Readiness 边界

M0 验证了进程组装、端口监听、嵌入 SPA、Metrics 和 Admin 鉴权边界。当前 `/admin/health` 仍是受 AdminOnly 保护的静态 `{"status":"ok"}`，不检查 Store、Provider、配置或迁移，不得称为 readiness。

任务书明确禁止 Task 10 提前实施 M1，而双 Server 的 `/health/live` 与 `/health/ready` 属于 Task 15。因此 M0 不伪造 readiness 成功；它将“无可用 readiness Endpoint”作为 M1 必须消除的明示风险。在 Task 15 完成前不允许将当前进程标记为生产就绪。

## 5. 首次失败与 Remediation

| 候选 | 现象 | 根因 | 处理 |
| --- | --- | --- | --- |
| `c3a9cd9` | 全新 Windows `TestLoadErrorGolden/unknown-field` 失败 | Golden 检出为 CRLF，运行时错误为 LF | Task 10.R1 在 Git 边界固定 `*.golden eol=lf` |
| `e7e4fd8` | 新检出后 Fixture `CR=0, LF=2` | 换行不再携带主机差异 | 全仓 Test/Race 与全部门禁通过 |

## 6. 残余风险与后续责任

| 风险 | 当前约束 | 责任 Task |
| --- | --- | --- |
| 单 Server，无真实 liveness/readiness | 只允许开发/演示启动 Smoke | Task 15 |
| SSE 解析/扫描失败可能被当作正常结束 | 保留 Task 7 Fixture，不声称流完整性 | Task 17、22 |
| 旧 Chat 模型无 Tools/Reasoning/Multimodal | 不往 `provider.ChatRequest` 堆厂商字段 | Task 19～27 |
| Ark/DeepSeek/Qwen 只有 disabled/unverified Schema | 不执行真实 API，不宣称已支持 | Task 28～36 |
| Fallback 与 Redis 降级缺少能力/多实例不变量 | 保留已知基线，不作为生产保证 | Task 37～44、56 |
| Quota 非原子、月额度未拒绝、Store 故障存在 Fail Open | 保留并发/故障保护测试，不声称账务正确 | Task 45～52 |
| Admin 无 OpenAPI/OIDC，Metrics 公开 | 当前只用于 standalone 开发演示 | Task 15、47、53～54 |
| Recharts 2.x 已弃用 | 锁文件保持可重复，不在 Gate 中夹带 UI 大版本迁移 | 后续前端功能 Task 评估 |

## 7. 最终放行决定

以下放行条件已经全部满足：

1. Task 9、Task 10.R1 和 Gate 报告已推送到远端。
2. GitHub-hosted `Quality` Workflow 在 Gate 报告提交上的 Go quality、Frontend quality、Secret scan 三个 Job 全部为 `success`。
3. Workflow URL 和 Head SHA 已记录在本文。
4. Task 10 已标记为 Done，Task 11 已释放为 Ready。

**决定：M0 Passed，允许开始 M1；下一工作单元为 Task 11～18。**
